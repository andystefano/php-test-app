apiVersion: v1
kind: Service
metadata:
  name: aplicacion-service
  labels:
    app: aplicacion
spec:
  type: LoadBalancer
  externalTrafficPolicy: Cluster
  selector:
    app: aplicacion
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
