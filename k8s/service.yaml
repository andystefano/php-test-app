apiVersion: v1
kind: Service
metadata:
  name: aplicacion-service
  labels:
    app: aplicacion
spec:
  type: LoadBalancer
  selector:
    app: aplicacion
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
